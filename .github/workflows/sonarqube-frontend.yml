name: SonarQube Front-end

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  sonar-frontend:
    runs-on: ubuntu-latest
    env:
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: 'front-end/pnpm-lock.yaml'

      - name: Setup Java
        id: setup-java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Check Java version
        run: java -version

      - name: Instalar dependencias
        run: pnpm install
        working-directory: front-end

      - name: Construir front-end
        run: pnpm build
        working-directory: front-end

      - name: Descargar SonarQube Scanner CLI
        run: |
          curl -sSLo sonar-scanner-cli.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.8.0.2856-linux.zip
          unzip sonar-scanner-cli.zip
        working-directory: front-end

      - name: Analizar front-end en SonarQube
        env:
          JAVA_HOME: ${{ steps.setup-java.outputs.java-home }}
          PATH: ${{ steps.setup-java.outputs.java-home }}/bin:${{ env.PATH }}
        run: |
          ./sonar-scanner-4.8.0.2856-linux/bin/sonar-scanner \
            -Dsonar.projectKey=gestion_pruebas_frontend \
            -Dsonar.projectName=gestion_pruebas_frontend \
            -Dsonar.sources=src \
            -Dsonar.exclusions=**/node_modules/**,android/** \
            -Dsonar.host.url=${{ env.SONAR_HOST_URL }} \
            -Dsonar.login=${{ env.SONAR_TOKEN }}
        working-directory: front-end
